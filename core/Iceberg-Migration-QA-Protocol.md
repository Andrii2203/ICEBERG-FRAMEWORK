# Migration Validation Protocol
**Мета:** Безпечний, верифікований перехід legacy→Next.js з збереженням функціональності 1-в-1.

## STOP CONDITIONS
Агент ПОВИНЕН зупинитися і запросити підтвердження при:

### 1. Невідповідність КОНТРАКТІВ A-E
- Якщо output не відповідає Definition of Done з Contract E
- Якщо порушено єдину термінологію (Contract A)
- Якщо спроба перескочити крок (Contract B)
- Якщо формат відповіді не відповідає специфікації (Contract C)
- Якщо зроблено припущення при нестачі даних (Contract D)

**Дія при STOP:**
- Чітко вказати, який контракт порушено
- Показати конкретний пункт Definition of Done, що не виконано
- Запропонувати виправлення
- НЕ продовжувати без підтвердження

### 2. Критична неповнота даних (>30% сторінок)
- Якщо в Repo A більше 30% файлів недоступні або некоректні
- Якщо структура маршрутизації незрозуміла після аналізу
- Якщо бізнес-логіка непрозора для >30% сторінок

**Дія при STOP:**
```
⚠️ STOP — Критична неповнота даних для [КРОК X]

Проблема:
- [Конкретний опис проблеми з відсотком]

Відсутні/неясні дані:
1. [Конкретний опис]
2. [Конкретний опис]

Приклад того, що потрібно:
[Конкретний приклад структури/файлів]

Варіанти дій:
A) Надати відсутні дані
B) Продовжити з частковою інформацією (ризик: [опис])
C) Змінити підхід до [КРОК X]

Чекаю вказівки.
```

### 3. Конфлікт архітектури Repo A vs Repo B
- Якщо існуючий код у Repo B (≈25% реалізації) суперечить оптимальній архітектурі з Кроку 4
- Якщо для збереження існуючого коду потрібні компроміси якості >20%

**Дія при STOP:**
```
⚠️ STOP — Конфлікт архітектури (КРОК 4)

Проблема:
Існуючий код у Repo B не відповідає оптимальній архітектурі.

Конфлікти:
1. [Файл/модуль]: [опис конфлікту]
2. [Файл/модуль]: [опис конфлікту]

Варіанти:
A) Переписати [X%] існуючого коду для відповідності новій архітектурі
   - Ризик: [час, помилки]
   - Переваги: [чиста архітектура]

B) Адаптувати архітектуру до існуючого коду
   - Ризик: [технічний борг]
   - Переваги: [збереження роботи]

C) Гібридний підхід: [опис]

Рекомендація: [A/B/C] тому що [обґрунтування]

Чекаю рішення.
```

### 4. Виявлення "покращень" або нових фіч
- Якщо в процесі аналізу виявлені можливості "покращити" legacy
- Якщо запропоновані зміни виходять за межі "1-в-1 відтворення"

**Дія при STOP:**
- Позначити виявлені можливості покращення як "Future Enhancements"
- НЕ включати їх у поточний план
- Створити окремий документ FUTURE_ENHANCEMENTS.md
- Продовжити без цих змін

### 5. Невідповідність термінології між кроками
- Якщо у Кроці 2 використано назву "Dashboard", а у Кроці 4 — "Main Page"
- Якщо словник термінів не оновлюється послідовно

**Дія при STOP:**
- Вказати всі випадки невідповідності
- Запропонувати єдину термінологію
- Оновити весь попередній output
- Продовжити після виправлення

---

## Обов'язкові API/Інтерфейси

### Структура Кроку 1 (Page Mapping)
Обов'язкові поля для кожної сторінки:
```typescript
interface PageMapping {
  pageName: string;        // Єдина назва (із словника)
  url: string;             // Повний маршрут
  pathToFiles: string[];   // Абсолютні шляхи
  shortDescription: string; // 1-2 речення, без бізнес-логіки
}
```

### Структура Кроку 2 (Deep Analysis)
Обов'язкові 8 категорій для кожної сторінки:
1. Бізнес-логіка
2. Мінімально необхідний функціонал
3. Дані
4. Логіка (Server/Client/Hooks/Services/Utils)
5. Компонентна структура
6. Повторне використання
7. Антипатерни legacy
8. План перепису

### Структура Кроку 3 (Existing Code Analysis)
Обов'язкові поля для кожного файлу:
```typescript
interface FileAnalysis {
  file: string;
  purpose: string;
  implemented: string;
  missing: string;
  needsRewrite: string;
  canBeReused: string;
}
```

### Структура Кроку 4 (Architecture)
Обов'язкові розділи:
- Project Folder Structure (дерево)
- Опис кожної ключової папки:
  - Purpose
  - What goes here
  - What does NOT go here
- Data flow diagram
- Boundaries of responsibility

### Структура Кроку 5 (Coding Rules)
Обов'язкові 7 категорій:
1. Стиль коду
2. Компонентні правила
3. Server/Client правила
4. Hooks/Services/Utils
5. API правила
6. Naming rules
7. Forbidden patterns

### Структура Кроку 6 (Master Execution Plan)
Обов'язкові 9 фаз:
1. Підготовка проєкту
2. Інфраструктура
3. Дані та API
4. Реалізація сторінок
5. Інтеграція
6. Тестування
7. Оптимізація
8. Документація
9. Фінальна збірка

Для кожного кроку у фазі:
```typescript
interface ExecutionStep {
  stepNumber: string;      // "1.1", "2.3", etc.
  description: string;
  input: string;           // Що вже має бути
  output: string;          // Конкретний результат
  executor: string;        // Людина/ШІ/будь-хто
  stopCheck: string;       // Критерії перевірки
}
```

---

## Single Source of Truth Principle
- **Repo A** — джерело поточної поведінки (що робить сайт зараз)
- **Repo B** — джерело майбутньої реалізації (як це має бути реалізовано)
- Аналіз завжди починається з Repo A
- Архітектура завжди формується для Repo B з урахуванням існуючого коду

## UI Read Rule (для майбутньої реалізації)
- Server Components MUST бути default
- Client Components тільки коли необхідно ('use client')
- Мінімум клієнтської логіки
- Максимум server-side rendering

## Server/Client Split Rules
- Server Components:
  - Отримання даних
  - Бізнес-логіка
  - Початкова обробка
- Client Components:
  - Інтерактивність (onClick, onChange)
  - Локальний стан (useState, useReducer)
  - Браузерні API

## Idempotency Rule
- Кожен крок протоколу має бути ідемпотентним
- Повторне виконання кроку з тими самими вхідними даними → той самий результат
- Якщо крок виконано частково — можливість перезапуску без втрати прогресу

---

## Execution Rules

### Принцип "No Magic"
- Уникати магічних абстракцій
- Явна, зрозуміла логіка
- Кожне рішення має бути обґрунтованим

### Принцип "Traceable Logic"
- Кожен крок протоколу має чіткий input/output
- Кожне рішення в архітектурі має обґрунтування
- Термінологія єдина і зрозуміла

### Принцип "Observable Progress"
- Кожен крок завершується STOP-CHECK
- Definition of Done перевіряється явно
- Прогрес вимірюваний (% завершення, кількість сторінок/файлів)

---

## Task Plan (агент повинен представити перед будь-якими змінами)

### Pre-execution Checklist
1. ✅ Прочитано Contracts A-E (Document 2)
2. ✅ Прочитано інструкцію для поточного кроку (Document 1)
3. ✅ Зрозуміло Definition of Done
4. ✅ Зрозуміло формат output
5. ✅ Підготовлено запити для нестачі даних (якщо потрібно)

### Execution Plan Structure
```
═══════════════════════════════════════════════════════════
ПЛАН ВИКОНАННЯ [КРОК X]
═══════════════════════════════════════════════════════════

📋 Вхідні дані (Input):
- [Список необхідних даних/файлів]

🎯 Мета (Goal):
- [Чітке формулювання мети кроку]

📊 Definition of Done:
- [ ] [Пункт 1]
- [ ] [Пункт 2]
- [ ] [...]

🔧 Дії (Actions):
1. [Дія 1]: [опис]
2. [Дія 2]: [опис]
3. [...]

📤 Вихідні дані (Output):
- [Формат і структура результату]

⚠️ Ризики:
- [Ризик 1]: [як мінімізувати]
- [Ризик 2]: [як мінімізувати]

🛑 STOP-CHECK:
- [Критерій 1 для перевірки]
- [Критерій 2 для перевірки]

═══════════════════════════════════════════════════════════

Чекаю підтвердження перед початком виконання.
```

---

## Output Requirements

### Перед виконанням:
- Надрукувати план (структура вище)
- Вказати точний список файлів для читання/зміни
- Чекати підтвердження

### Після виконання:
- Список змінених файлів із короткими описами
- Архівовані файли (якщо є)
- Git команди для відтворення
- Результати перевірки Definition of Done
- Запит на підтвердження перед переходом до наступного кроку

### При виконанні STOP condition:
```
⚠️ STOP — [Назва умови]

Діагностика:
- [Детальний опис проблеми]
- [Конкретні дані: файли, рядки, метрики]

Пропоновані наступні кроки:
A) [Варіант A]
   - Переваги: [...]
   - Ризики: [...]

B) [Варіант B]
   - Переваги: [...]
   - Ризики: [...]

Рекомендація: [A/B] тому що [обґрунтування]

НЕ ПРОДОВЖУВАТИ БЕЗ ПІДТВЕРДЖЕННЯ.
```

---

## Constraints

### Абсолютні заборони:
- ❌ Видаляти файли з Repo A (legacy) без бекапу
- ❌ Змінювати код у Repo B без плану і підтвердження
- ❌ Переходити до наступного кроку без STOP-CHECK
- ❌ Робити припущення при нестачі даних
- ❌ Вигадувати нові фічі або "покращення"
- ❌ Використовувати різні назви для однієї сутності

### Дозволені дії:
- ✅ Читати будь-які файли в обох репозиторіях
- ✅ Створювати документацію (analysis, plans, diagrams)
- ✅ Пропонувати альтернативні підходи з обґрунтуванням
- ✅ Ставити уточнюючі запитання
- ✅ Зупинятися при невизначеності

---

## End State

Очікуваний результат після виконання всіх 6 кроків:

### Repo A (Legacy) — НЕЗМІНЕНИЙ
- Використовується тільки як референс
- Залишається доступним для уточнень під час усього процесу

### Repo B (New Next.js) — PRODUCTION-READY
- Чиста архітектура (затверджена в Кроці 4)
- Кодова база відповідає Coding Rules (Крок 5)
- Всі сторінки реалізовані згідно Master Execution Plan (Крок 6)
- Функціональність 1-в-1 відповідає legacy
- Без legacy-хаосу
- Документація актуальна
- Готово до продакшену

### Документація
- ARCHITECTURE.md — опис архітектури
- CODING_RULES.md — правила реалізації
- EXECUTION_PLAN.md — покроковий план (виконаний)
- TERMINOLOGY.md — словник термінів
- FUTURE_ENHANCEMENTS.md — що можна покращити пізніше (опціонально)

---

## Format of Responses

### Структура відповіді для будь-якого кроку:
```
═══════════════════════════════════════════════════════════
[КРОК X] — [НАЗВА КРОКУ]
═══════════════════════════════════════════════════════════

[Основний контент згідно формату кроку]

───────────────────────────────────────────────────────────

📊 DEFINITION OF DONE — ПЕРЕВІРКА

- [✅/❌] [Пункт 1]: [статус]
- [✅/❌] [Пункт 2]: [статус]
- [✅/❌] [...]

───────────────────────────────────────────────────────────

📖 СЛОВНИК ТЕРМІНІВ (ОНОВЛЕНИЙ)

[Термін 1] = [Єдина назва]
[Термін 2] = [Єдина назва]
[...]

───────────────────────────────────────────────────────────

🛑 STOP-CHECK

Крок [X] завершено.
Чекаю підтвердження перед переходом до Кроку [X+1].

═══════════════════════════════════════════════════════════
```

### Логи мають бути структуровані:
- JSON-формат для автоматичної обробки (опціонально)
- Трасування (який крок, який файл, яка дія)
- Часові мітки (опціонально)

---

## Risk Management

### High-risk Operations
1. Переписування існуючого коду Repo B (Крок 3 → 4)
2. Зміна архітектури після Кроку 4
3. Масове видалення файлів

**Для high-risk операцій:**
- Детальний план із ризиками
- Явне підтвердження користувача
- Можливість rollback (якщо застосовно)

### Low-risk Operations
- Читання файлів
- Створення документації
- Аналіз без змін

---

## Appendix: Quick Reference

### STOP Conditions Summary
1. ⚠️ Невідповідність Contracts A-E
2. ⚠️ Критична неповнота даних (>30%)
3. ⚠️ Конфлікт архітектури Repo A vs Repo B
4. ⚠️ Виявлення "покращень" або нових фіч
5. ⚠️ Невідповідність термінології між кроками

### Definition of Done Quick Check
- КРОК 1: [ ] Всі маршрути, [ ] 4 поля, [ ] Дерево
- КРОК 2: [ ] Кожна сторінка, [ ] 8 категорій, [ ] План
- КРОК 3: [ ] Всі файли, [ ] 6 полів, [ ] Coverage
- КРОК 4: [ ] Дерево папок, [ ] Data flow, [ ] Масштабування
- КРОК 5: [ ] 7 категорій, [ ] Чіткі правила
- КРОК 6: [ ] 9 фаз, [ ] Input/Output/STOP-CHECK

### Emergency Procedures
Якщо щось пішло не так:
- Зупинитися та оцінити ситуацію
- Повідомити про проблему з деталями
- Запропонувати варіанти виходу
- Не продовжувати без підтвердження

---

**END OF MIGRATION VALIDATION PROTOCOL**
